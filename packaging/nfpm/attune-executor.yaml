# nfpm configuration for attune-executor
name: attune-executor
arch: "${ARCH}"
platform: linux
version: "${VERSION}"
section: admin
priority: optional
maintainer: "Attune <attune@rdrx.app>"
description: "Attune Executor — Execution orchestration and workflow engine"
vendor: "Attune"
homepage: "https://git.rdrx.app/attune/attune"
license: "Proprietary"

depends:
  - attune-common (= ${VERSION})
  - libc6 (>= 2.28)
  - postgresql-16
  - timescaledb-2-postgresql-16
  - rabbitmq-server

contents:
  - src: "${BUNDLE_DIR}/bin/attune-executor"
    dst: /usr/bin/attune-executor
    file_info:
      mode: 0755

  - src: ../systemd/attune-executor.service
    dst: /usr/lib/systemd/system/attune-executor.service
    file_info:
      mode: 0644

scripts:
  preinstall: ../scripts/preinstall-attune-executor.sh
  postinstall: ../scripts/postinstall-attune-executor.sh
  preremove: ../scripts/preremove-attune-executor.sh
  postremove: ../scripts/postremove.sh

overrides:
  deb:
    deb:
      predepends:
        - attune-common (= ${VERSION})
  rpm:
    depends:
      - attune-common = ${VERSION}
      - glibc >= 2.28
      - postgresql16-server
      - timescaledb-2-postgresql-16
      - rabbitmq-server
    rpm:
      scripts:
        posttrans: ../scripts/postupgrade-attune-executor.sh
  archlinux:
    depends:
      - attune-common=${VERSION}
      - glibc
      - postgresql
      - timescaledb
      - rabbitmq
    archlinux:
      scripts:
        preupgrade: ../scripts/preinstall-attune-executor.sh
        postupgrade: ../scripts/postupgrade-attune-executor.sh
