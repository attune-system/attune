// React Router v8 provides the browser bindings from the main package. The
// Vite alias mirrors this declaration at runtime while legacy imports migrate.
declare module "react-router-dom" {
  export * from "react-router";
}
